import firebase from '../firebase';
import { GET_FB_USER } from './types';


/*  
    Take a snapshot of database and sort the payload by votes. Must convert firebase 
    snapshot to Object.values [{}] 
*/
export const getUser = () => dispatch => {
    
}
